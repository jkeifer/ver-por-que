// Type surface for the generated src/build-info.js (written by
// scripts/get-git-info.js at build time; gitignored). Hand-written so tsc and
// eslint don't need allowJs to consume the generated module.
export declare const BUILD_INFO: {
    commit: string;
    buildTime: string;
};
